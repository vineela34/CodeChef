#include<stdio.h>
int main(){
	int x;
	float y;
	scanf("%d",&x);
	scanf("%f",&y);
	if(y>(x+0.5)){
		if(x % 5==0){
			y=y-x-0.5;
			printf("%.2f",y);
		}
		else{
			printf("%.2f",y);
	
		}
	}
	else{
		printf("%.2f",y);
	}
	return 0;

}
